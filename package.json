{
  "name": "titan-pulse-capacitor",
  "version": "1.0.0",
  "private": true,
  "description": "TITAN PULSE Android application packaged with Capacitor 8",
  "scripts": {
    "build:web": "node scripts/check-web.js",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:run": "npx cap run android",
    "android:open": "npx cap open android",
    "check:production": "node scripts/check-production.js",
    "test:static": "npm run build:web && npm run check:production"
  },
  "dependencies": {
    "@capacitor/core": "8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.0",
    "@capacitor/android": "8.5.0",
    "typescript": "^5.9.3"
  }
}
